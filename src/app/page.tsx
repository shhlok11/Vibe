import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return (
    <div>
      <Button className="p-4 m-4 bg-indigo-800">
        Click Me
      </Button>
    </div>
  );
};

export default page;
